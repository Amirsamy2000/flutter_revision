import 'package:flutter/material.dart';
class ListingWidgets extends StatelessWidget{
  const ListingWidgets({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
   return const MaterialApp(
home: Scaffold(

)
   );
  }
  
 

}